namespace CensusProcessing.Enums
{
    public enum ProcessType
    {
        I, // Inclusion
        T, // Termination
        S, // Suspension
        V, // Vessel Change
        M, // Manning Change
        C  // Client Transfer
    }
}
